# Backend environment
DATABASE_URL=sqlite+aiosqlite:///./app.db   # for dev; Docker overrides via compose
# DATABASE_URL=postgresql+asyncpg://postgres:example@db:5432/justfarming
MQTT_BROKER_URL=mosquitto
MQTT_BROKER_PORT=1883
# Allow local dev (Vite) and dockerized frontend (nginx on :80)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost,http://127.0.0.1
